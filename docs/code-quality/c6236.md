---
title: C6236
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6236
helpviewer_keywords:
- C6236
ms.assetid: 3d5ae268-6f40-4c45-a483-b5b0e6a808fc
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 2a8dba90f6814df6b032eeb0f89a7d265926399e
ms.sourcegitcommit: 535ef05b1e553f0fc66082cd2e0998817eb2a56a
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/07/2019
ms.locfileid: "72018132"
---
# <a name="c6236"></a>C6236
Warning C6236: (\<expression > &#124; &#124; \<Non-nulová konstanta >) je vždy nenulová konstanta.

 Toto upozornění znamená, že na pravé straně logického nebo operace, která se vyskytuje v kontextu testu, byla zjištěna nenulová konstantní hodnota, která je jiná než jedna. Logicky to znamená, že test je redundantní a lze jej bezpečně odebrat. Případně navrhuje, aby programátor mohl použít jiný operátor, například rovnost (`==`), bitový operátor AND (`&`) nebo bitový operátor XOR (`^`), k otestování konkrétní hodnoty nebo příznaku.

 Toto upozornění se negeneruje pro Common idiom, pokud je nenulová konstanta 1, protože je k dispozici pro selektivní povolení cest kódu v době kompilace. Nicméně upozornění je vygenerováno, pokud je nenulová konstanta vytvořena výrazem, který je vyhodnocen jako 1, například 1 + 0.

## <a name="example"></a>Příklad
 Tento kód ukazuje, jak se může zobrazit upozornění C6236. Vzhledem k tomu, že `INPUT_TYPE` není 0, je výraz `n || INPUT_TYPE` vždy nenulový a klauzule `else` se nikdy neprovede. @No__t-0 je však konstanta s jinou hodnotou než 1, což naznačuje, že je určena jako hodnota pro porovnání:

```
#define INPUT_TYPE 2
#include <stdio.h>

void f( int n )
{
   if ( n || INPUT_TYPE ) // analysis warning C6236
   {
      puts( "Always gets here" );
   }
   else
   {
      puts( "Never enters here" );
   }
}
```

 Následující kód místo toho používá operátor bitového operátoru AND (`&`) k otestování, zda je nastavena `INPUT_TYPE` bit vstupního parametru `n`:

```
#define INPUT_TYPE 2
#include <stdio.h>

void f( int n )
{
   if ( n & INPUT_TYPE ) // no warning
   {
      puts( "Bitwise-AND comparison is true" );
   }
   else
   {
      puts( "Bitwise-AND comparison is false" );
   }
}
```

## <a name="see-also"></a>Viz také
 [C++Bitové operátory](http://go.microsoft.com/fwlink/?LinkId=181162)