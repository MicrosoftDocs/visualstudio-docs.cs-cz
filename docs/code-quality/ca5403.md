---
title: 'CA5403: Nepoužívejte pevně zakódovaný certifikát'
description: Obsahuje informace o CA5403 pravidla analýzy kódu, včetně příčin, jak opravit porušení a kdy je potlačit.
ms.date: 10/16/2019
ms.topic: reference
author: LLLXXXCCC
ms.author: linche
manager: jillfra
dev_langs:
- CSharp
ms.workload:
- multiple
f1_keywords:
- CA5403
ms.openlocfilehash: 1d007ac34969ef5edd2b07d03993df8b655f421a
ms.sourcegitcommit: a18c7e9b367c2f92f6e54c3eaef442775d457667
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 09/15/2020
ms.locfileid: "90094470"
---
# <a name="ca5403-do-not-hard-code-certificate"></a>CA5403: Nepoužívejte pevně zakódovaný certifikát

|Položka|Hodnota|
|-|-|
|RuleId|CA5403|
|Kategorie|Microsoft.Security|
|Zásadní změna|Nenarušující|

## <a name="cause"></a>Příčina

`data`Parametr nebo `rawData` <xref:System.Security.Cryptography.X509Certificates.X509Certificate> <xref:System.Security.Cryptography.X509Certificates.X509Certificate2> konstruktoru nebo je pevně zakódován jedním z následujících způsobů:

- Bajtové pole.
- Pole znaků.
- <xref:System.Convert.FromBase64String(System.String)?displayProperty=fullName>.
- Všechna přetížení <xref:System.Text.Encoding.GetBytes%2A?displayProperty=fullName> .

## <a name="rule-description"></a>Popis pravidla

Snadno se zjistí soukromý klíč pevně zakódovaného certifikátu. I s zkompilovanými binárními soubory je snadné uživatelům se zlými úmysly extrahovat soukromý klíč pevně zakódovaného certifikátu. Po ohrožení bezpečnosti privátního klíče útočník může tento certifikát zosobnit a veškeré prostředky a operace chráněné tímto certifikátem budou k dispozici útočníkovi.

## <a name="how-to-fix-violations"></a>Jak opravit porušení

- Zvažte přenávrh aplikace tak, aby používala zabezpečený systém správy klíčů, jako je například Azure Key Vault.
- Přihlašovací údaje a certifikáty Uchovávejte v zabezpečeném umístění odděleně od zdrojového kódu.

## <a name="when-to-suppress-warnings"></a>Kdy potlačit upozornění

Pokud pevně zakódovaná data neobsahují privátní klíč certifikátu, je bezpečné potlačit upozornění od tohoto pravidla. Data jsou například ze `.cer` souboru. Hardwarové informace o veřejných certifikátech můžou pořád vytvořit výzvu pro střídání certifikátů, pokud vyprší nebo odvolají.

## <a name="pseudo-code-examples"></a>Příklady kódu pseudo

### <a name="hard-coded-by-byte-array"></a>Pevně zakódované podle bajtového pole

```csharp
using System.IO;
using System.Security.Cryptography.X509Certificates;

class ExampleClass
{
    public void ExampleMethod(string path)
    {
        byte[] bytes = new byte[] {1, 2, 3};
        File.WriteAllBytes(path, bytes);
        new X509Certificate2(path);
    }
}
```

### <a name="hard-coded-by-char-array"></a>Pevně zakódované podle pole char

```csharp
using System.IO;
using System.Security.Cryptography.X509Certificates;
using System.Text;

class ExampleClass
{
    public void ExampleMethod(byte[] bytes, string path)
    {
        char[] chars = new char[] { '1', '2', '3' };
        Encoding.ASCII.GetBytes(chars, 0, 3, bytes, 0);
        File.WriteAllBytes(path, bytes);
        new X509Certificate2(path);
    }
}
```

### <a name="hard-coded-by-frombase64string"></a>Pevně zakódovaný podle FromBase64String

```csharp
using System;
using System.IO;
using System.Security.Cryptography.X509Certificates;

class ExampleClass
{
    public void ExampleMethod(string path)
    {
        byte[] bytes = Convert.FromBase64String("AAAAAaazaoensuth");
        File.WriteAllBytes(path, bytes);
        new X509Certificate2(path);
    }
}
```

### <a name="hard-coded-by-getbytes"></a>Pevně zakódované podle GetBytes

```csharp
using System;
using System.IO;
using System.Security.Cryptography.X509Certificates;
using System.Text;

class ExampleClass
{
    public void ExampleMethod(string path)
    {
        byte[] bytes = Encoding.ASCII.GetBytes("AAAAAaazaoensuth");
        File.WriteAllBytes(path, bytes);
        new X509Certificate2(path);
    }
}
```

### <a name="solution"></a>Řešení

```csharp
using System.IO;
using System.Security.Cryptography.X509Certificates;

class ExampleClass
{
    public void ExampleMethod(string path)
    {
        new X509Certificate2("Certificate.cer");
    }
}
```
