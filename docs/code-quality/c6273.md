---
title: C6273
ms.date: 11/04/2016
ms.topic: reference
f1_keywords:
- C6273
helpviewer_keywords:
- C6273
ms.assetid: e780e504-8b8d-4d61-b15f-4859133134ad
author: mikeblome
ms.author: mblome
manager: markl
ms.workload:
- multiple
ms.openlocfilehash: 6e3f01560218f960935cfcc22e0066a22ebe52d5
ms.sourcegitcommit: 5f6ad1cefbcd3d531ce587ad30e684684f4c4d44
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 10/22/2019
ms.locfileid: "72746547"
---
# <a name="c6273"></a>C6273
Upozornění 6273 – hodnota bez celého čísla předaná jako parametr \<number >, pokud je při volání \<function > vyžadováno celé číslo: Pokud je předávána hodnota ukazatele, měla by se použít% p.

 Toto upozornění označuje, že formátovací řetězec Určuje celé číslo, například specifikace `%d`, délka nebo priority pro `printf`, ale jiné než celé číslo, jako je například `float`, řetězec nebo `struct`, se předává jako parametr. Tato vada může mít za následek nesprávný výstup.

## <a name="example"></a>Příklad
 Následující kód vygeneruje toto upozornění, protože místo `float` do `sprintf`function je vyžadováno celé číslo.

```
#include <stdio.h>
#include <string.h>

void f_defective()
{
  char buff[50];
  float f=1.5;

  sprintf(buff, "%d",f);
}
```

 Následující kód používá přetypování Integer k opravě tohoto upozornění:

```
#include <stdio.h>
#include <string.h>

void f_corrected()
{
  char buff[50];
  float f=1.5;

  sprintf(buff,"%d",(int)f);
}
```

 Následující kód používá funkci bezpečného zpracování řetězce, `sprintf_s` pro opravu tohoto upozornění:

```
#include <stdio.h>
#include <string.h>

void f_safe()
{
  char buff[50];
  float f=1.5;

  sprintf_s(buff,50,"%d",(int)f);
}
```

 Toto upozornění se nevztahuje na systémy Windows 9x a Windows NT verze 4, protože% p není na těchto platformách podporováno.

## <a name="see-also"></a>Viz také:
 [sprintf –, _sprintf_l, swprintf, _swprintf_l, \__swprintf_l](/cpp/c-runtime-library/reference/sprintf-sprintf-l-swprintf-swprintf-l-swprintf-l)
